import { answerRatings } from 'ask-anything/helpers/answer-ratings';
import { module, test } from 'qunit';

module('Unit | Helper | answer ratings');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = answerRatings([42]);
  assert.ok(result);
});
