import { timeStamp } from 'ask-anything/helpers/time-stamp';
import { module, test } from 'qunit';

module('Unit | Helper | time stamp');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = timeStamp([42]);
  assert.ok(result);
});
